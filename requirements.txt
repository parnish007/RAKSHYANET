# RakshyaNet backend -- runtime dependencies.
#
# This file is what the deployed serverless function installs, so it lists only
# what the request path actually imports. Vercel caps a function bundle at
# 250 MB unzipped and scientific wheels are large, so anything unused here is
# not merely untidy -- it is a deployment failure waiting to happen.
#
# Development and test tooling lives in requirements-dev.txt.
# Python 3.10+

# Web framework (starlette arrives as a fastapi dependency)
fastapi==0.110.0
pydantic==2.6.4
python-multipart==0.0.9

# Outbound calls to the Gemma API
httpx==0.27.0

# Optimization -- scipy.optimize.minimize backs the social welfare optimizer
scipy==1.12.0
numpy==1.26.4

# Routing / graph
networkx==3.3

# Utilities
python-dotenv==1.0.1

# Deliberately NOT here:
#   pandas, pulp  -- imported nowhere under backend/; carried over from an
#                    earlier design, and together they cost ~100 MB of bundle.
#   uvicorn       -- a local dev server. The platform supplies the ASGI server
#                    in production. See requirements-dev.txt.
#   websockets    -- uvicorn's WS transport. A serverless function cannot hold a
#                    socket open, so the deployed build has no use for it.
#   aiofiles      -- no longer imported anywhere.
#   pyserial      -- backend/p2p/bridge_manager.py imports it behind a guarded
#                    try/except for optional LoRa hardware, absent by design.
