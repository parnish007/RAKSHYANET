# Mathematical Model

The authoritative mathematical description is now
[`MATH.md`](../MATH.md). It covers the complete current pipeline:

- evidence-grounded Gemma inputs and deterministic system confidence;
- the bounded Gemma-to-urgency handoff;
- auditable urgency components;
- closure-filtered Dijkstra routing for ground assets;
- direct geodesic routing for aircraft;
- payload, compatibility, ETA, and fuel constraints;
- capped proportional allocation and normalized convergence;
- weighted Nash social-welfare comparison;
- scoped KKT diagnostics;
- approval invariants and UI display requirements.

This short file remains only to preserve existing documentation links. Do not
copy formulas from an older commit without checking the root document and the
implementation.
